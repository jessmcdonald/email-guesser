import React from 'react';
import renderer from 'react-test-renderer';
import Form from '../Form/Form';

describe("FormComponent", () => {
    it('validates the full name input on change', () => {

    });
    it('validates the full name input on change', () => {

    });
    it('validates the full name input on change', () => {

    });
})